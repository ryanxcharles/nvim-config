return {
  {
    "akinsho/bufferline.nvim",
    enabled = false,
    -- version = "*", -- Use the latest version
    -- dependencies = {
    --   "nvim-tree/nvim-web-devicons", -- Optional: for file icons
    -- },
    -- event = "VeryLazy", -- Load bufferline.nvim after startup for performance
    -- config = function()
    --   -- Enable true colors
    --   vim.o.termguicolors = true
    --
    --   -- Configure bufferline
    --   require("bufferline").setup({
    --     options = {
    --       mode = "buffers", -- Display buffers (use "tabs" for tab mode)
    --       separator_style = "thin", -- Separator style: "slant", "thick", "thin", etc.
    --       always_show_bufferline = true, -- Always show bufferline
    --       show_buffer_close_icons = true, -- Show close icons
    --       color_icons = true, -- Enable colored icons
    --     },
    --     highlights = {
    --       fill = {
    --         bg = "#222434", -- Background color for bufferline's empty space
    --       },
    --       -- background = {
    --       --   bg = "#24283b", -- Background for inactive buffers
    --       -- },
    --       -- tab = {
    --       --   bg = "#24283b", -- Background for inactive tabs
    --       -- },
    --       -- tab_selected = {
    --       --   bg = "#3e4451", -- Background for selected tab
    --       -- },
    --       -- buffer_visible = {
    --       --   bg = "#24283b", -- Background for visible but inactive buffers
    --       -- },
    --       -- buffer_selected = {
    --       --   bg = "#3e4451", -- Background for selected buffer
    --       -- },
    --     },
    --   })
    -- end,
  },
}
